"""承诺抽取 Prompt。

只对客服最终发送的消息生效 —— 客服没发出的草稿不进入承诺跟踪。
模型只负责「读懂表达了什么、时间原话是什么」，截止时间由 time.py 归一化。
"""

PROMISE_EXTRACT_PROMPT = """
你是客服服务承诺抽取助手。请判断下面这条客服消息里，是否包含对消费者的明确承诺。

客服消息：
{message}

判断口径：
1. 只有客服主动给出的、可执行的服务动作才算承诺，例如退款、补发、换货、回访、物流跟进。
2. 单纯道歉、安抚、解释流程，不算承诺。
3. 消费者自己提的要求而客服没有答应，不算承诺。
4. due_expression 必须原样摘录消息里的时间表达（如「明天上午」「3个工作日」「72小时内」），
   没有时间表达就留空。**不要自己换算或推断具体日期**。

请严格按照 json 格式输出。

{format_instructions}
"""
